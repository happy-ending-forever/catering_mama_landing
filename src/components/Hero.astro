---
import { Star, ChevronRight, CheckCircle } from "lucide-astro";

interface Props {
  whatsappLink: string;
}

const { whatsappLink } = Astro.props;
---

<header class="relative flex items-center pt-28 md:pt-28 pb-8 md:pb-16">
  <!-- Desktop Layout -->
  <div
    class="container mx-auto px-6 hidden md:grid md:grid-cols-2 gap-10 lg:gap-20 items-center"
  >
    <div class="space-y-8 animate-fade-in-up">
      <h1 class="text-5xl lg:text-6xl font-bold leading-[1.15] tracking-tight">
        Sajian <span class="text-yellow-500">Lezat</span> Dengan Cinta Dari Mama.
      </h1>

      <div>
        <div
          class="inline-flex items-center gap-2 bg-yellow-50 text-yellow-700 px-4 py-2 rounded-full text-sm font-semibold border border-yellow-100"
        >
          <Star class="w-4 h-4 fill-yellow-500 text-yellow-500" />
          Katering #1 Pilihan Keluarga
        </div>
      </div>

      <p class="text-lg text-slate-600 max-w-lg leading-relaxed">
        Menghadirkan rasa otentik rumahan dengan kualitas premium untuk setiap
        momen spesial Anda. Higienis, halal, dan tepat waktu.
      </p>

      <div class="flex flex-row gap-4 pt-2">
        <a
          href={whatsappLink}
          class="flex items-center justify-center gap-2 bg-yellow-400 hover:bg-yellow-500 text-white px-8 py-4 rounded-2xl font-bold text-lg transition-all shadow-xl shadow-yellow-200 hover:-translate-y-1"
        >
          Pesan Sekarang <ChevronRight class="w-5 h-5" />
        </a>
        <a
          href="#menu"
          class="flex items-center justify-center gap-2 bg-white border-2 border-slate-100 hover:border-yellow-200 px-8 py-4 rounded-2xl font-bold text-lg transition-all"
        >
          Lihat Menu
        </a>
      </div>

      <div
        class="flex items-center gap-4 text-sm text-slate-500 pt-6 border-t border-slate-50"
      >
        <div class="flex -space-x-3">
          {
            [11, 12, 13, 14].map((img) => (
              <img
                src={`https://i.pravatar.cc/100?img=${img}`}
                alt="User"
                class="w-10 h-10 rounded-full border-2 border-white bg-slate-200"
                loading="lazy"
              />
            ))
          }
        </div>
        <span class="font-medium text-sm"
          >Dipesan oleh 1,000+ pelanggan puas bulan ini</span
        >
      </div>
    </div>

    <div class="relative flex justify-center lg:justify-end items-center">
      <div
        class="absolute inset-0 md:-inset-10 bg-yellow-400/10 rounded-full blur-3xl -z-10"
      >
      </div>
      <div class="relative w-full max-w-sm lg:max-w-md">
        <div
          class="rounded-[3rem] overflow-hidden shadow-2xl -rotate-2 hover:rotate-0 transition-transform duration-700"
        >
          <img
            src="https://images.unsplash.com/photo-1555244162-803834f70033?q=80&w=1000&auto=format&fit=crop"
            alt="Hidangan lezat dari Catering Mama"
            class="w-full h-[650px] object-cover"
            loading="eager"
          />
        </div>
        <div
          class="absolute -bottom-6 -left-12 bg-white p-6 rounded-3xl shadow-2xl animate-bounce-slow border border-slate-50"
        >
          <div class="flex items-center gap-4">
            <div class="bg-green-100 p-3 rounded-2xl">
              <CheckCircle class="w-6 h-6 text-green-600" />
            </div>
            <div>
              <p class="font-bold text-base">100% Halal</p>
              <p class="text-xs text-slate-500 font-medium">
                Terverifikasi MUI
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile Layout: Full Background Image with Centered Content -->
  <div
    class="md:hidden relative min-h-[100svh] flex items-center justify-center"
  >
    <!-- Background Image -->
    <div class="absolute inset-0 z-0">
      <img
        src="https://images.unsplash.com/photo-1490818387583-1baba5e638af?q=80&w=800&auto=format&fit=crop"
        alt="Background makanan"
        class="w-full h-full object-cover"
        loading="eager"
      />
      <div class="absolute inset-0 bg-white/85"></div>
    </div>

    <!-- Content Overlay -->
    <div class="relative z-10 px-6 py-20 text-center animate-fade-in-up">
      <!-- Title -->
      <h1
        class="text-4xl font-bold leading-[1.2] tracking-tight mb-6 text-slate-800"
      >
        Sajian <span class="text-yellow-500">Lezat</span><br />
        Dengan Cinta<br />
        Dari Mama
      </h1>

      <!-- Description -->
      <p class="text-base text-slate-600 leading-relaxed mb-8 max-w-xs mx-auto">
        Menghadirkan rasa otentik rumahan dengan kualitas premium untuk setiap
        momen spesial.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col gap-3 max-w-xs mx-auto">
        <a
          href={whatsappLink}
          class="flex items-center justify-center gap-2 bg-yellow-500 hover:bg-yellow-600 text-white px-6 py-4 rounded-full font-bold text-base transition-all shadow-lg"
        >
          Pesan Sekarang <ChevronRight class="w-4 h-4" />
        </a>
        <a
          href="#menu"
          class="flex items-center justify-center gap-2 bg-white border-2 border-slate-200 px-6 py-4 rounded-full font-bold text-base transition-all hover:border-yellow-300"
        >
          Lihat Menu
        </a>
      </div>

      <!-- Social Proof -->
      <div class="flex items-center justify-center gap-3 text-slate-500 mt-8">
        <div class="flex -space-x-2">
          {
            [11, 12, 13].map((img) => (
              <img
                src={`https://i.pravatar.cc/100?img=${img}`}
                alt="User"
                class="w-8 h-8 rounded-full border-2 border-white bg-slate-200"
                loading="lazy"
              />
            ))
          }
        </div>
        <span class="font-medium text-xs">1,000+ pelanggan puas</span>
      </div>
    </div>
  </div>
</header>
